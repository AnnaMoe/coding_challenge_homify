 <div class="col-12">
      <div class="bg-white rounded p-3">
        <h4>Add your todo</h4>
        <%= simple_form_for [@campaign, @todo] do |f| %>
          <%= f.input :name, placeholder: 'Add your todo here...' %>
          <%= f.submit class: 'btn btn-success' %>
        <% end %>
      </div>
    </div>